from app.models import Connection, db, SCHEMA, environment
from sqlalchemy.sql import text

def seed_connections():
  connection01 = Connection(id=1, user_id=1, 
    connection=""" """)

  connection01 = Connection(id=1, user_id=1, 
    connection=""" """)
  
  connection01 = Connection(id=1, user_id=1, 
    connection=""" """)
  
  connection01 = Connection(id=1, user_id=1, 
    connection=""" """)